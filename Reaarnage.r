arrange_ascending <- function(numbers) {
  sorted_numbers <- sort(numbers)
  return(sorted_numbers)
}

# Example usage:
numbers <- c(5, 2, 8, 1, 9)
arranged_numbers <- arrange_ascending(numbers)
print(arranged_numbers)
